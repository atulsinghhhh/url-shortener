version: '3.9'

services:
  redis:
    image: redis:latest
    container_name: redis
    ports: 
      - "6379:6379"
    volumes:
      - redis_data:/redis_data
    restart: always

volumes:
  redis_data